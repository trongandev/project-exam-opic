export const BASE_URL = 'https://opic.io.vn'

export const dynamicRoutes = async () => {
    // Fetch dynamic routes from API or database if required
    return []
}

export const excludeRoutes = ['/admin', '/private']
